[package]
name = "base64"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0"
authors = ["Alex Morales", "SFWTools"]
description = "Encode a string to base64."

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
